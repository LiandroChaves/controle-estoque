import FinalizarCompras from "../../components/modals/finalizarCompras";

export default function PageFinalizarCompras() {
    return <FinalizarCompras 
        isOpen={true} 
        setIsOpen={() => {}} 
        vendas={[]} 
        atualizarLista={() => {}} 
    />
}
